import { readFile, writeFile } from 'node:fs/promises'
import path from 'node:path'

const root = path.resolve('research/slot-time-300ms-boundary')
const input = path.join(root, 'analysis.json')
const output = path.join(root, 'final-report.md')
const scaleDistribution = (distribution: Record<string, number | null>) => Object.fromEntries(Object.entries(distribution).map(([key, value]) => [key, typeof value === 'number' && key !== 'count' ? value * .6 : value]))
function corrected(value: any, limit: number) {
  value.perBlock.applicableBlockCuLimit = limit
  value.perBlock.allLandedCuUtilization = scaleDistribution(value.perBlock.allLandedCuUtilization)
  value.perBlock.nonVoteCuShareOfApplicableLimit = scaleDistribution(value.perBlock.nonVoteCuShareOfApplicableLimit)
}
function percent(value: number | null | undefined) { return value == null ? 'UNKNOWN' : `${(value * 100).toFixed(2)}%` }
function delta(before: number, after: number) { return `${((after - before) / before * 100).toFixed(1)}%` }
function row(label: string, before: number | null, after: number | null, format: (x: number) => string = x => x.toLocaleString()) { return `| ${label} | ${before == null ? 'UNKNOWN' : format(before)} | ${after == null ? 'UNKNOWN' : format(after)} | ${before == null || after == null ? 'UNKNOWN' : delta(before, after)} |` }

async function main() {
const report = JSON.parse(await readFile(input, 'utf8'))
for (const [group, limit] of [[report.primary60Minutes, { before: 87_500_000, after: 75_000_000 }], [report.robustness3Hours, { before: 87_500_000, after: 75_000_000 }]] as const) {
  corrected(group.before, limit.before); corrected(group.after, limit.after); for (const bucket of group.beforeBuckets) corrected(bucket, limit.before); for (const bucket of group.afterBuckets) corrected(bucket, limit.after)
}
report.methodology.limits = { before350ms: 87_500_000, after300ms: 75_000_000, evidence: 'SIMD-0286 100M baseline scaled by SIMD-0525 target-slot-time ratios.' }
report.generatedAt = new Date().toISOString()
await writeFile(input, `${JSON.stringify(report, null, 2)}\n`)
const b = report.primary60Minutes.before, a = report.primary60Minutes.after
const bucketRows = (side: any) => side.map((x: any) => `| ${x.index} | ${x.sampledBlocks} | ${x.sampledLandedNonVoteTransactions.toLocaleString()} | ${percent(x.success.rate)} | ${percent(x.computeUnits.failedCuShare)} | ${x.computeUnits.distribution.median.toLocaleString()} | ${x.perBlock.transactions.median.toLocaleString()} | ${percent(x.perBlock.allLandedCuUtilization.median)} |`).join('\n')
const markdown = `# Solana 350ms → 300ms: landed non-vote execution\n\n## Boundary and scope\n\n- **Effective boundary:** epoch 1024, slot 442,368,000, \`${new Date(report.boundary.boundaryBlockTime * 1000).toISOString()}\`.\n- **Pre side:** epoch 1023 at the 350ms stage; **post side:** epoch 1024 at the 300ms stage.\n- **Population:** landed transactions in deterministic sampled blocks; vote-only transactions are excluded when every outer instruction is Vote or Compute Budget and at least one is Vote.\n- **No transition buffer:** the sides are separated exactly at the epoch boundary.\n- **Capacity denominator:** 87.5M CUs before and 75M after: the active 100M baseline scaled by SIMD-0525.\n\n## Methodology\n\nThe primary comparison uses two symmetric 60-minute wall-clock windows. Each side contains 60 deterministically selected, evenly ranked landed blocks. The robustness comparison uses two 3-hour windows and 180 blocks per side. Selection used only block position; success state, fee, compute, programs, signers, and all post-landing information played no role.\n\nAll counts below are **sampled-ledger counts**, not network totals. A transaction is either landed-success or landed-failed based on its receipt. This study cannot see transactions that never landed.\n\n## Primary 60-minute comparison\n\n| Observed dimension | Before | After | Relative difference |\n| --- | ---: | ---: | ---: |\n${row('Sampled blocks', b.sampledBlocks, a.sampledBlocks)}\n${row('Sampled landed non-vote transactions', b.sampledLandedNonVoteTransactions, a.sampledLandedNonVoteTransactions)}\n${row('Success rate', b.success.rate, a.success.rate, x => percent(x))}\n${row('Failure rate', b.failure.rate, a.failure.rate, x => percent(x))}\n${row('Total consumed CUs', b.computeUnits.total, a.computeUnits.total)}\n${row('Failed-CU share', b.computeUnits.failedCuShare, a.computeUnits.failedCuShare, x => percent(x))}\n${row('Median CUs / transaction', b.computeUnits.distribution.median, a.computeUnits.distribution.median)}\n${row('Median cost units / transaction', b.costUnits.distribution.median, a.costUnits.distribution.median)}\n${row('Total fees (lamports)', b.feesLamports.total, a.feesLamports.total)}\n${row('Median native priority fee (reconstructable subset)', b.priorityFeeLamports.distribution.median, a.priorityFeeLamports.distribution.median)}\n${row('Median non-vote transactions / block', b.perBlock.transactions.median, a.perBlock.transactions.median)}\n${row('Median non-vote CUs / block', b.perBlock.nonVoteCu.median, a.perBlock.nonVoteCu.median)}\n${row('Median all-landed CU utilization', b.perBlock.allLandedCuUtilization.median, a.perBlock.allLandedCuUtilization.median, x => percent(x))}\n\nPriority fees were reconstructable for ${percent(b.priorityFeeLamports.reconstructableRate)} before and ${percent(a.priorityFeeLamports.reconstructableRate)} after. Cost units were available for all sampled non-vote transactions. Cost units and consumed CUs remain distinct runtime measures; no causal or ratio meaning is assigned to their relationship, despite observed correlation.\n\n## 10-minute chronological buckets\n\n### Before\n\n| Bucket | Blocks | Sampled non-vote tx | Success rate | Failed-CU share | Median CU/tx | Median tx/block | Median all-landed CU utilization |\n| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |\n${bucketRows(report.primary60Minutes.beforeBuckets)}\n\n### After\n\n| Bucket | Blocks | Sampled non-vote tx | Success rate | Failed-CU share | Median CU/tx | Median tx/block | Median all-landed CU utilization |\n| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |\n${bucketRows(report.primary60Minutes.afterBuckets)}\n\n## 3-hour robustness check\n\nThe 3-hour sample changes direction on several aggregate measures: success rate is ${percent(report.robustness3Hours.before.success.rate)} before versus ${percent(report.robustness3Hours.after.success.rate)} after; sampled fee total is ${report.robustness3Hours.before.feesLamports.total.toLocaleString()} versus ${report.robustness3Hours.after.feesLamports.total.toLocaleString()}; median transaction CUs are ${report.robustness3Hours.before.computeUnits.distribution.median.toLocaleString()} versus ${report.robustness3Hours.after.computeUnits.distribution.median.toLocaleString()}. This is evidence against calling the 60-minute fee or success-rate differences a stable boundary effect.\n\n## What the ledger establishes\n\nIt establishes receipt outcomes, consumed CUs, cost units, fees, reconstructed native priority fees where the relevant Compute Budget instructions are present, and their sampled block-level distribution around the boundary. It also exposes meaningful variation across short chronological buckets that a single aggregate would hide.\n\n## What it does not establish\n\nIt does not establish sender geography, network path, sender targeting, dropped traffic, end-to-end submission latency, application class, address identity, or why any measured dimension moved. Differences may reflect changing transaction mix, leaders, fee-market conditions, time-of-day, concurrent feature changes, or sample variation.\n\n## Verdict\n\n**INTERESTING — NEED MORE WINDOW.** The sample shows changes in ledger-visible execution mix and resource use, but the short-window fee and success movements do not persist in the same direction in the three-hour robustness check. This is not strong enough to send back publicly as a slot-time causal result.\n`
await writeFile(output, markdown)
console.log(`Wrote ${output}`)
}
void main()
